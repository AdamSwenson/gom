/**
 * This connects with the serer api and handles all incoming
 * and outgoing requests.
 *
 * Created by adam on 3/20/17.
 */

import * as mTypes from '../mutation-types'
import * as aTypes from '../action-types'

const state = {};

const mutations = {};

const actions = {};

const getters = {};


export default {
    actions,
    getters,
    mutations,
    state,
}