<?xml version="1.0" encoding="UTF-8"?>
<project name="gradeomatic3" default="build">
    <!-- By default, we assume all tools to be on the $PATH -->
    <!--<property name="toolsdir" value=""/>-->

    <!-- Uncomment the following when the tools are in ${basedir}/vendor/bin -->
    <property name="toolsdir" value="${basedir}/vendor/bin/"/>


    <target name="build"
            depends="composer,prepare,lint,phploc-ci,pdepend,phpmd-ci,phpcs-ci,phpcpd-ci,phpunit,phpcb"
            description=""/>


    <!--<target name="build"-->
            <!--depends="composer,prepare,lint,phploc-ci,pdepend,phpmd-ci,phpcs-ci,phpcpd-ci,phpdox,phpunit,phpcb"-->
            <!--description=""/>-->
    <!--<target name="build"-->
    <!--depends="composer,prepare,lint,phploc-ci,pdepend,phpmd-ci,phpcs-ci,phpcpd-ci,phpdox,phpunit,codeceptAccept,phpcb"-->
    <!--description=""/>-->

    <!--<target name="build-parallel"-->
    <!--depends="prepare,lint,tools-parallel,phpunit,codeceptSetup,codeceptGrade,codeceptReport,codeceptAdmin,phpcb"-->
    <!--description=""/>-->

    <target name="build-parallel"
            depends="prepare,lint,tools-parallel"
            description=""/>


    <target name="tools-parallel" description="Run tools in parallel">
        <parallel threadCount="2">
            <sequential>
                <antcall target="pdepend"/>
                <antcall target="phpmd-ci"/>
            </sequential>
            <antcall target="phpcpd-ci"/>
            <antcall target="phpcs-ci"/>
            <antcall target="phploc-ci"/>
        </parallel>
    </target>

    <target name="composer" description="Installing composer dependencies">
        <exec executable="composer" failonerror="true">
            <arg value="install"/>
            <arg value="--dev"/>
            <arg value="--prefer-dist"/>
            <arg value="--no-progress"/>

            <env key="SYMFONY_ENV" value="test"/>
        </exec>
    </target>

    <target name="codeceptAccept"
            depends="prepare"
            description="Run codeception acceptance tests">
        <exec executable="${basedir}/vendor/bin/codecept">
            <env key="APP_ENV" value="codeceptWorld"/>
            <arg value="run"/>
            <arg value="acceptance"/>
            <arg value="--html"/>
            <arg value="--colors"/>
            <arg value="--xml"/>
            <arg value="--coverage-html"/>
        </exec>
    </target>

    <target name="codeceptSetup"
            depends="prepare"
            description="Run codeception tests for setup">
        <exec executable="${basedir}/vendor/bin/codecept">
            <env key="APP_ENV" value="codeceptWorld"/>
            <arg value="run"/>
            <arg value="tests/acceptance/setup"/>
            <arg value="--html"/>
            <arg value="--colors"/>
            <arg value="--xml"/>
            <arg value="--coverage-html"/>
        </exec>
    </target>

    <target name="codeceptGrade"
            depends="prepare"
            description="Run codeception tests for grading">
        <exec executable="${basedir}/vendor/bin/codecept">
            <env key="APP_ENV" value="codeceptWorld"/>
            <arg value="run"/>
            <arg value="tests/acceptance/grade"/>
            <arg value="--html"/>
            <arg value="--colors"/>
            <arg value="--xml"/>
            <arg value="--coverage-html"/>
        </exec>
    </target>

    <target name="codeceptReport"
            depends="prepare"
            description="Run codeception tests for reporting">
        <exec executable="${basedir}/vendor/bin/codecept">
            <env key="APP_ENV" value="codeceptWorld"/>
            <arg value="run"/>
            <arg value="tests/acceptance/report"/>
            <arg value="--html"/>
            <arg value="--colors"/>
            <arg value="--xml"/>
            <arg value="--coverage-html"/>
        </exec>
    </target>

    <target name="codeceptAdmin"
            depends="prepare"
            description="Run codeception tests for admin">
        <exec executable="${basedir}/vendor/bin/codecept">
            <env key="APP_ENV" value="codeceptWorld"/>
            <arg value="run"/>
            <arg value="tests/acceptance/admin"/>
            <arg value="--html"/>
            <arg value="--colors"/>
            <arg value="--xml"/>
            <arg value="--coverage-html"/>
        </exec>
    </target>

    <target name="clean"
            unless="clean.done"
            description="Cleanup build artifacts">
        <delete dir="${basedir}/build/api"/>
        <delete dir="${basedir}/build/logs"/>
        <delete dir="${basedir}/build/pdepend"/>
        <property name="clean.done" value="true"/>
    </target>

    <target name="prepare"
            unless="prepare.done"
            depends="clean"
            description="Prepare for build">
        <mkdir dir="${basedir}/build/api"/>
        <mkdir dir="${basedir}/build/logs"/>
        <mkdir dir="${basedir}/build/pdepend"/>
        <property name="prepare.done" value="true"/>
    </target>

    <target name="lint" description="Perform syntax check of sourcecode files">
        <apply executable="php" failonerror="false">
            <arg value="-l"/>
            <fileset dir="${basedir}/app">
                <include name="**/*.php"/>
                <exclude name="generated-classes/**"/>
                <exclude name="generated-conf/**"/>
                <exclude name="generated-sql/**"/>
                <exclude name="models/**"/>
                <modified/>
            </fileset>

            <fileset dir="${basedir}/tests">
                <include name="**/*.php"/>
                <modified/>
            </fileset>
        </apply>
    </target>

    <target name="phpcb"
            depends="prepare"
            description="Run codebrowser on output">
        <exec executable="${toolsdir}phpcb">
            <arg line="--log ${basedir}/build/logs/
                    --ignore ${basedir}/tests/
                   --output ${basedir}/build/code-browser/
                   --source ${basedir}"/>
        </exec>
    </target>

    <!--<target name="phpcb" description="Aggregate tool output with PHP_CodeBrowser">-->
    <!--<exec executable="${toolsdir}phpcb">-->
    <!--<arg value="&#45;&#45;log" />-->
    <!--<arg path="${basedir}/build/logs" />-->
    <!--<arg value="&#45;&#45;ignore" />-->
    <!--<arg path="${basedir}/tests" />-->
    <!--<arg value="&#45;&#45;source" />-->
    <!--<arg path="${basedir}" />-->
    <!--<arg value="&#45;&#45;output" />-->
    <!--<arg path="${basedir}/build/code-browser" />-->
    <!--</exec>-->
    <!--</target>-->

    <target name="phploc"
            description="Measure project size using PHPLOC and print human readable output. Intended for usage on the command line.">
        <exec executable="${toolsdir}phploc">
            <arg value="--count-tests"/>
            <arg path="${basedir}/app"/>
            <arg path="${basedir}/tests"/>
        </exec>
    </target>

    <target name="phploc-ci"
            depends="prepare"
            description="Measure project size using PHPLOC and log result in CSV and XML format. Intended for usage within a continuous integration environment.">
        <exec executable="${toolsdir}phploc">
            <arg value="--count-tests"/>
            <arg value="--log-csv"/>
            <arg path="${basedir}/build/logs/phploc.csv"/>
            <arg value="--log-xml"/>
            <arg path="${basedir}/build/logs/phploc.xml"/>
            <arg path="${basedir}/app"/>
            <arg path="${basedir}/tests"/>
        </exec>
    </target>

    <target name="pdepend"
            depends="prepare"
            description="Calculate software metrics using PHP_Depend and log result in XML format. Intended for usage within a continuous integration environment.">
        <exec executable="${toolsdir}pdepend">
            <arg value="--jdepend-xml=${basedir}/build/logs/jdepend.xml"/>
            <arg value="--jdepend-chart=${basedir}/build/pdepend/dependencies.svg"/>
            <arg value="--overview-pyramid=${basedir}/build/pdepend/overview-pyramid.svg"/>
            <arg path="${basedir}/app"/>
        </exec>
    </target>

    <target name="phpmd"
            description="Perform project mess detection using PHPMD and print human readable output. Intended for usage on the command line before committing.">
        <exec executable="${toolsdir}phpmd">
            <arg path="${basedir}/app"/>
            <arg value="text"/>
            <arg path="${basedir}/phpmd.xml"/>
        </exec>
    </target>

    <target name="phpmd-ci"
            depends="prepare"
            description="Perform project mess detection using PHPMD and log result in XML format. Intended for usage within a continuous integration environment.">
        <exec executable="${toolsdir}phpmd">
            <arg path="${basedir}/app"/>
            <arg value="xml"/>
            <arg path="${basedir}/phpmd.xml"/>
            <arg value="--reportfile"/>
            <arg path="${basedir}/build/logs/pmd.xml"/>
        </exec>
    </target>

    <target name="phpcs"
            description="Find coding standard violations using PHP_CodeSniffer and print human readable output. Intended for usage on the command line before committing.">
        <exec executable="${toolsdir}phpcs">
            <arg value="--standard=PSR2"/>
            <arg value="--extensions=php"/>
            <arg value="--ignore=autoload.php"/>
            <arg path="${basedir}/app"/>
            <arg path="${basedir}/tests"/>
        </exec>
    </target>

    <target name="phpcs-ci"
            depends="prepare"
            description="Find coding standard violations using PHP_CodeSniffer and log result in XML format. Intended for usage within a continuous integration environment.">
        <exec executable="${toolsdir}phpcs" output="/dev/null">
            <arg value="--report=checkstyle"/>
            <arg value="--report-file=${basedir}/build/logs/checkstyle.xml"/>
            <arg value="--standard=PSR2"/>
            <arg value="--extensions=php"/>
            <arg value="--ignore=autoload.php"/>
            <arg value="--ignore=vendor"/>
            <arg path="${basedir}/app"/>
        </exec>
    </target>

    <target name="phpcpd"
            description="Find duplicate code using PHPCPD and print human readable output. Intended for usage on the command line before committing.">
        <exec executable="${toolsdir}phpcpd">
            <arg path="${basedir}/app"/>
        </exec>
    </target>

    <target name="phpcpd-ci"
            depends="prepare"
            description="Find duplicate code using PHPCPD and log result in XML format. Intended for usage within a continuous integration environment.">
        <exec executable="${toolsdir}phpcpd">
            <arg value="--log-pmd"/>
            <arg path="${basedir}/build/logs/pmd-cpd.xml"/>
            <arg path="${basedir}/app"/>
        </exec>
    </target>

    <target name="phpunit"
            depends="prepare"
            description="Run unit tests with PHPUnit">
        <exec executable="${toolsdir}phpunit" failonerror="false">
            <arg value="--configuration"/>
            <arg path="${basedir}/phpunit.xml"/>
        </exec>
    </target>

    <target name="phpdox"
            depends="phploc-ci,phpcs-ci,phpmd-ci"
            description="Generate project documentation using phpDox">
        <exec executable="${toolsdir}phpdox">
            <arg value="--file"/>
            <arg path="${basedir}/phpdox.xml"/>
        </exec>
    </target>

</project>