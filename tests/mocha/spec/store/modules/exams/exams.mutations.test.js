
//The name of the tested component
var compName = 'exams.mutations';
//The path to the tested component
var Component = require( '../../../../../../resources/assets/js/store/modules/exams/exams.mutations.js' );


require( '../../../../injectglobals' );

//tested object

describe( compName, () => {
    let listOfValues, test;
    let payload, exam, item, kumi, kumis, student, grade;
    
    beforeEach( () => {

    } );


    describe( " actions", () => {
        it.skip( 'awaits tests', () => {
             } );
    } );


    describe( " getters", () => {
        it.skip( 'awaits tests', () => {
        } );
    } );


    describe( " mutations ", () => {
        it.skip( 'awaits tests', () => {
        } );
    } );

} );
