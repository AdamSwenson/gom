require('jasmine-jquery');
require('sinon');

//Dependencies
import * as active from '../../../../../resources/assets/js/store/modules/grade.grades';

import * as types from '../../../../../resources/assets/js/store/mutation-types'



describe("store | modules | ", () => {
    describe("grade.grades | ", () => {
        beforeAll(function () {
//runs once before all tests
        });
        afterEach(function () {
//runs after each test
        });

        beforeEach(function () {
//runs before each test
        });


        describe("mutations | ", () => {
        });

        describe("actions | ", () => {
        });

        describe("getters | ", () => {
        });
    });
});
